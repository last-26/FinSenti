[project]
name = "finsenti-training"
version = "0.1.0"
description = "Financial Sentiment Analysis Training Pipeline"
requires-python = ">=3.11"

[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "W"]
